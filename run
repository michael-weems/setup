#!/usr/bin/env bash

source ./.bashlib # TODO: expand this
SCRIPT_DIR=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )

if [ -z "$XDG_CONFIG_HOME" ]; then
    echo "no xdg config home, defaulting to: ~/.config"
    XDG_CONFIG_HOME=$HOME/.config
fi

pushd ${SCRIPT_DIR}

os=""
filter=""
for ((i=0; i<argc; i++)); do
	if   [[ "-os" == "${argv[$i]}" ]]; then
		i=$((i + 1))
		os="${argv[$i]}"
	elif [[ "-g" == "${argv[$i]}" ]] || [[ "-grep" == "${argv[$i]}" ]]; then
		i=$((i + 1))
		filter="${argv[$i]}"
	fi
done

if [[ -z "$os" ]]; then
	echo "please provide an os: arch, wsl-ubuntu, windows, fedora"
	exit 1
fi



echo "run: $filter"

scripts=$(find ./os/${os}/pkg -maxdepth 1 -mindepth 1 -executable -type f | sort) # find every executable in the runs dir
echo "scripts: ${scripts}"

for script in $scripts; do
      echo "script: ${script}"
	# does this script not contain this filter
	if echo "$script" | grep -qv "$filter"; then
		echo "filtering $script"
		continue
	fi
	./$script
done

echo "done: please run the below command"
echo ""
echo "    cd ~/projects/dotfiles && ./reconfigure"
echo ""

popd
